spring:
  application:
    name: paging-and-filtering
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100
logging:
  level:
    org.hibernate.SQL: DEBUG